            // if (document.getElementById("stage").innerText == `2/4`) {
            //     isRemoveItems = false
            // }

            // if (document.getElementById("stage").innerText == `2/4` && isRemoveItems === false) {
            //     sessionStorage.removeItem("collectionPlayer")
            //     isRemoveItems = true
            // }

            // if (document.getElementById("stage").innerText == `2/4` && !isGenerateItems) {
            //     generateItems({
            //         minY: 0,
            //         maxY: window.innerHeight - 168,
            //         minX: -50,
            //         maxX: window.innerWidth,
            //         itemAmount: 6
            //     })

            //     document.getElementById("item").innerText = `0/6`;

            //     console.log("item stage 2", currentLevel.item);
            //     isGenerateItems = true;
            //     const allObjects = [...currentLevel.item];

            //     const itemDetec = itemsDetectorColl(allObjects)

            //     currentLevel.player.collectItems(itemDetec, currentLevel.name, input, currentLevel.item)

            //     // if (coll !== null) {
            //     //     document.getElementById("item").innerText = `${JSON.parse(coll.length)}/6`;
            //     // } else {
            //     //     document.getElementById("item").innerText = `0/6`;
            //     // }

            // }
